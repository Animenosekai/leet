# The Python Environment

The python environment in LeetCode.

## Globals

Here are the globals that are available in the Python environment:
<details>
  <summary>Global Variables</summary>
  <ul>
    <li><code>ascii_letters</code> (<code>str</code>)</li>
    <li><code>ascii_lowercase</code> (<code>str</code>)</li>
    <li><code>ascii_uppercase</code> (<code>str</code>)</li>
    <li><code>capwords</code> (<code>function</code>) (from 'string')</li>
    <li><code>digits</code> (<code>str</code>)</li>
    <li><code>hexdigits</code> (<code>str</code>)</li>
    <li><code>octdigits</code> (<code>str</code>)</li>
    <li><code>printable</code> (<code>str</code>)</li>
    <li><code>punctuation</code> (<code>str</code>)</li>
    <li><code>whitespace</code> (<code>str</code>)</li>
    <li><code>Formatter</code> (<code>type</code>) (from 'string')</li>
    <li><code>Template</code> (<code>type</code>) (from 'string')</li>
    <li><code>match</code> (<code>function</code>) (from 're')</li>
    <li><code>fullmatch</code> (<code>function</code>) (from 're')</li>
    <li><code>search</code> (<code>function</code>) (from 're')</li>
    <li><code>sub</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>subn</code> (<code>function</code>) (from 're')</li>
    <li><code>split</code> (<code>function</code>) (from 're')</li>
    <li><code>findall</code> (<code>function</code>) (from 're')</li>
    <li><code>finditer</code> (<code>function</code>) (from 're')</li>
    <li><code>compile</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>purge</code> (<code>function</code>) (from 're')</li>
    <li><code>template</code> (<code>function</code>) (from 're')</li>
    <li><code>escape</code> (<code>function</code>) (from 're')</li>
    <li><code>error</code> (<code>type</code>) (from 're')</li>
    <li><code>Pattern</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Match</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>A</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>I</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>L</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>M</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>S</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>X</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>U</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>ASCII</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>IGNORECASE</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>LOCALE</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>MULTILINE</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>DOTALL</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>VERBOSE</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>UNICODE</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>NOFLAG</code> (<code>RegexFlag</code>) (from 're')</li>
    <li><code>RegexFlag</code> (<code>EnumType</code>) (from 're')</li>
    <li><code>date</code> (<code>type</code>) (from 'datetime')</li>
    <li><code>datetime</code> (<code>module</code>)</li>
    <li><code>time</code> (<code>type</code>) (from 'datetime')</li>
    <li><code>timedelta</code> (<code>type</code>) (from 'datetime')</li>
    <li><code>timezone</code> (<code>type</code>) (from 'datetime')</li>
    <li><code>tzinfo</code> (<code>type</code>) (from 'datetime')</li>
    <li><code>MINYEAR</code> (<code>int</code>)</li>
    <li><code>MAXYEAR</code> (<code>int</code>)</li>
    <li><code>UTC</code> (<code>timezone</code>)</li>
    <li><code>ChainMap</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Counter</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>OrderedDict</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>UserDict</code> (<code>ABCMeta</code>) (from 'collections')</li>
    <li><code>UserList</code> (<code>ABCMeta</code>) (from 'collections')</li>
    <li><code>UserString</code> (<code>ABCMeta</code>) (from 'collections')</li>
    <li><code>defaultdict</code> (<code>type</code>) (from 'collections')</li>
    <li><code>deque</code> (<code>type</code>) (from 'collections')</li>
    <li><code>namedtuple</code> (<code>function</code>) (from 'collections')</li>
    <li><code>heappush</code> (<code>builtin_function_or_method</code>) (from '_heapq')</li>
    <li><code>heappop</code> (<code>builtin_function_or_method</code>) (from '_heapq')</li>
    <li><code>heapify</code> (<code>builtin_function_or_method</code>) (from '_heapq')</li>
    <li><code>heacodeplace</code> (<code>builtin_function_or_method</code>) (from '_heapq')</li>
    <li><code>merge</code> (<code>function</code>) (from 'heapq')</li>
    <li><code>nlargest</code> (<code>function</code>) (from 'heapq')</li>
    <li><code>nsmallest</code> (<code>function</code>) (from 'heapq')</li>
    <li><code>heappushpop</code> (<code>builtin_function_or_method</code>) (from '_heapq')</li>
    <li><code>insort_right</code> (<code>builtin_function_or_method</code>) (from '_bisect')</li>
    <li><code>bisect_right</code> (<code>builtin_function_or_method</code>) (from '_bisect')</li>
    <li><code>insort_left</code> (<code>builtin_function_or_method</code>) (from '_bisect')</li>
    <li><code>bisect_left</code> (<code>builtin_function_or_method</code>) (from '_bisect')</li>
    <li><code>bisect</code> (<code>module</code>)</li>
    <li><code>insort</code> (<code>builtin_function_or_method</code>) (from '_bisect')</li>
    <li><code>Error</code> (<code>type</code>) (from 'copy')</li>
    <li><code>copy</code> (<code>module</code>)</li>
    <li><code>deepcopy</code> (<code>function</code>) (from 'copy')</li>
    <li><code>acos</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>acosh</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>asin</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>asinh</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>atan</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>atan2</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>atanh</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>cbrt</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>ceil</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>copysign</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>cos</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>cosh</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>degrees</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>dist</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>erf</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>erfc</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>exp</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>exp2</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>expm1</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>fabs</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>factorial</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>floor</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>fmod</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>frexp</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>fsum</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>gamma</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>gcd</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>hypot</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>isclose</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>isfinite</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>isinf</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>isnan</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>isqrt</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>lcm</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>ldexp</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>lgamma</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>log</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>log1p</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>log10</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>log2</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>modf</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>pow</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>radians</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>remainder</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>sin</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>sinh</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>sqrt</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>tan</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>tanh</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>trunc</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>prod</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>perm</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>comb</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>nextafter</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>ulp</code> (<code>builtin_function_or_method</code>) (from 'math')</li>
    <li><code>pi</code> (<code>float</code>)</li>
    <li><code>e</code> (<code>float</code>)</li>
    <li><code>tau</code> (<code>float</code>)</li>
    <li><code>inf</code> (<code>float</code>)</li>
    <li><code>nan</code> (<code>float</code>)</li>
    <li><code>Random</code> (<code>type</code>) (from 'random')</li>
    <li><code>SystemRandom</code> (<code>type</code>) (from 'random')</li>
    <li><code>betavariate</code> (<code>method</code>) (from 'random')</li>
    <li><code>choice</code> (<code>method</code>) (from 'random')</li>
    <li><code>choices</code> (<code>method</code>) (from 'random')</li>
    <li><code>expovariate</code> (<code>method</code>) (from 'random')</li>
    <li><code>gammavariate</code> (<code>method</code>) (from 'random')</li>
    <li><code>gauss</code> (<code>method</code>) (from 'random')</li>
    <li><code>getrandbits</code> (<code>builtin_function_or_method</code>) (from 'None')</li>
    <li><code>getstate</code> (<code>method</code>) (from 'random')</li>
    <li><code>lognormvariate</code> (<code>method</code>) (from 'random')</li>
    <li><code>normalvariate</code> (<code>method</code>) (from 'random')</li>
    <li><code>paretovariate</code> (<code>method</code>) (from 'random')</li>
    <li><code>randbytes</code> (<code>method</code>) (from 'random')</li>
    <li><code>randint</code> (<code>method</code>) (from 'random')</li>
    <li><code>random</code> (<code>module</code>)</li>
    <li><code>randrange</code> (<code>method</code>) (from 'random')</li>
    <li><code>sample</code> (<code>method</code>) (from 'random')</li>
    <li><code>seed</code> (<code>method</code>) (from 'random')</li>
    <li><code>setstate</code> (<code>method</code>) (from 'random')</li>
    <li><code>shuffle</code> (<code>method</code>) (from 'random')</li>
    <li><code>triangular</code> (<code>method</code>) (from 'random')</li>
    <li><code>uniform</code> (<code>method</code>) (from 'random')</li>
    <li><code>vonmisesvariate</code> (<code>method</code>) (from 'random')</li>
    <li><code>weibullvariate</code> (<code>method</code>) (from 'random')</li>
    <li><code>NormalDist</code> (<code>type</code>) (from 'statistics')</li>
    <li><code>StatisticsError</code> (<code>type</code>) (from 'statistics')</li>
    <li><code>correlation</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>covariance</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>fmean</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>geometric_mean</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>harmonic_mean</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>linear_regression</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>mean</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>median</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>median_grouped</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>median_high</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>median_low</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>mode</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>multimode</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>pstdev</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>pvariance</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>quantiles</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>stdev</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>variance</code> (<code>function</code>) (from 'statistics')</li>
    <li><code>tee</code> (<code>builtin_function_or_method</code>) (from 'itertools')</li>
    <li><code>accumulate</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>combinations</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>combinations_with_replacement</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>cycle</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>dropwhile</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>takewhile</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>islice</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>starmap</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>chain</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>comcodess</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>filterfalse</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>count</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>zip_longest</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>pairwise</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>permutations</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>product</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>repeat</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>groupby</code> (<code>type</code>) (from 'itertools')</li>
    <li><code>update_wrapper</code> (<code>function</code>) (from 'functools')</li>
    <li><code>wraps</code> (<code>function</code>) (from 'functools')</li>
    <li><code>WRAPPER_ASSIGNMENTS</code> (<code>tuple</code>)</li>
    <li><code>WRAPPER_UPDATES</code> (<code>tuple</code>)</li>
    <li><code>total_ordering</code> (<code>function</code>) (from 'functools')</li>
    <li><code>cache</code> (<code>function</code>) (from 'functools')</li>
    <li><code>cmp_to_key</code> (<code>builtin_function_or_method</code>) (from '_functools')</li>
    <li><code>lru_cache</code> (<code>function</code>) (from 'functools')</li>
    <li><code>reduce</code> (<code>builtin_function_or_method</code>) (from '_functools')</li>
    <li><code>partial</code> (<code>type</code>) (from 'functools')</li>
    <li><code>partialmethod</code> (<code>type</code>) (from 'functools')</li>
    <li><code>singledispatch</code> (<code>function</code>) (from 'functools')</li>
    <li><code>singledispatchmethod</code> (<code>type</code>) (from 'functools')</li>
    <li><code>cached_property</code> (<code>type</code>) (from 'functools')</li>
    <li><code>abs</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>add</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>and_</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>attrgetter</code> (<code>type</code>) (from 'operator')</li>
    <li><code>call</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>concat</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>contains</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>countOf</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>delitem</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>eq</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>floordiv</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>ge</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>getitem</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>gt</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>iadd</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>iand</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>iconcat</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>ifloordiv</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>ilshift</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>imatmul</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>imod</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>imul</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>index</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>indexOf</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>inv</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>invert</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>ior</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>ipow</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>irshift</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>is_</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>is_not</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>isub</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>itemgetter</code> (<code>type</code>) (from 'operator')</li>
    <li><code>itruediv</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>ixor</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>le</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>length_hint</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>lshift</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>lt</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>matmul</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>methodcaller</code> (<code>type</code>) (from 'operator')</li>
    <li><code>mod</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>mul</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>ne</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>neg</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>not_</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>or_</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>pos</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>rshift</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>setitem</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>truediv</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>truth</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>xor</code> (<code>builtin_function_or_method</code>) (from '_operator')</li>
    <li><code>BlockingIOError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>open</code> (<code>builtin_function_or_method</code>) (from 'io')</li>
    <li><code>open_code</code> (<code>builtin_function_or_method</code>) (from 'io')</li>
    <li><code>IOBase</code> (<code>ABCMeta</code>) (from 'io')</li>
    <li><code>RawIOBase</code> (<code>ABCMeta</code>) (from 'io')</li>
    <li><code>FileIO</code> (<code>type</code>) (from '_io')</li>
    <li><code>BytesIO</code> (<code>type</code>) (from '_io')</li>
    <li><code>StringIO</code> (<code>type</code>) (from '_io')</li>
    <li><code>BufferedIOBase</code> (<code>ABCMeta</code>) (from 'io')</li>
    <li><code>BufferedReader</code> (<code>type</code>) (from '_io')</li>
    <li><code>BufferedWriter</code> (<code>type</code>) (from '_io')</li>
    <li><code>BufferedRWPair</code> (<code>type</code>) (from '_io')</li>
    <li><code>BufferedRandom</code> (<code>type</code>) (from '_io')</li>
    <li><code>TextIOBase</code> (<code>ABCMeta</code>) (from 'io')</li>
    <li><code>TextIOWrapper</code> (<code>type</code>) (from '_io')</li>
    <li><code>UnsupportedOperation</code> (<code>type</code>) (from 'io')</li>
    <li><code>SEEK_SET</code> (<code>int</code>)</li>
    <li><code>SEEK_CUR</code> (<code>int</code>)</li>
    <li><code>SEEK_END</code> (<code>int</code>)</li>
    <li><code>DEFAULT_BUFFER_SIZE</code> (<code>int</code>)</li>
    <li><code>text_encoding</code> (<code>builtin_function_or_method</code>) (from 'io')</li>
    <li><code>IncrementalNewlineDecoder</code> (<code>type</code>) (from '_io')</li>
    <li><code>addaudithook</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>audit</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>breakpointhook</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>displayhook</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>exception</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>exc_info</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>excepthook</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>exit</code> (<code>Quitter</code>) (from '_sitebuiltins')</li>
    <li><code>getdefaultencoding</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getdlopenflags</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getallocatedblocks</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getfilesystemencoding</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getfilesystemencodeerrors</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getrefcount</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getrecursionlimit</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getsizeof</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>intern</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>is_finalizing</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>setswitchinterval</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getswitchinterval</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>setdlopenflags</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>setprofile</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>getprofile</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>setrecursionlimit</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>settrace</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>gettrace</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>call_tracing</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>set_coroutine_origin_tracking_depth</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>get_coroutine_origin_tracking_depth</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>set_asyncgen_hooks</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>get_asyncgen_hooks</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>unraisablehook</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>get_int_max_str_digits</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>set_int_max_str_digits</code> (<code>builtin_function_or_method</code>) (from 'sys')</li>
    <li><code>modules</code> (<code>dict</code>)</li>
    <li><code>stderr</code> (<code>TextIOWrapper</code>)</li>
    <li><code>version</code> (<code>str</code>)</li>
    <li><code>hexversion</code> (<code>int</code>)</li>
    <li><code>api_version</code> (<code>int</code>)</li>
    <li><code>copyright</code> (<code>_Printer</code>) (from '_sitebuiltins')</li>
    <li><code>platform</code> (<code>str</code>)</li>
    <li><code>maxsize</code> (<code>int</code>)</li>
    <li><code>float_info</code> (<code>float_info</code>)</li>
    <li><code>int_info</code> (<code>int_info</code>)</li>
    <li><code>hash_info</code> (<code>hash_info</code>)</li>
    <li><code>maxunicode</code> (<code>int</code>)</li>
    <li><code>builtin_module_names</code> (<code>tuple</code>)</li>
    <li><code>stdlib_module_names</code> (<code>frozenset</code>)</li>
    <li><code>byteorder</code> (<code>str</code>)</li>
    <li><code>abiflags</code> (<code>str</code>)</li>
    <li><code>version_info</code> (<code>version_info</code>)</li>
    <li><code>implementation</code> (<code>SimpleNamespace</code>)</li>
    <li><code>flags</code> (<code>flags</code>)</li>
    <li><code>float_repr_style</code> (<code>str</code>)</li>
    <li><code>thread_info</code> (<code>thread_info</code>)</li>
    <li><code>meta_path</code> (<code>list</code>)</li>
    <li><code>path_importer_cache</code> (<code>dict</code>)</li>
    <li><code>path_hooks</code> (<code>list</code>)</li>
    <li><code>path</code> (<code>list</code>)</li>
    <li><code>executable</code> (<code>str</code>)</li>
    <li><code>codefix</code> (<code>str</code>)</li>
    <li><code>base_codefix</code> (<code>str</code>)</li>
    <li><code>exec_codefix</code> (<code>str</code>)</li>
    <li><code>base_exec_codefix</code> (<code>str</code>)</li>
    <li><code>platlibdir</code> (<code>str</code>)</li>
    <li><code>pycache_codefix</code> (<code>NoneType</code>)</li>
    <li><code>argv</code> (<code>list</code>)</li>
    <li><code>orig_argv</code> (<code>list</code>)</li>
    <li><code>warnoptions</code> (<code>list</code>)</li>
    <li><code>dont_write_bytecode</code> (<code>bool</code>)</li>
    <li><code>stdin</code> (<code>TextIOWrapper</code>)</li>
    <li><code>stdout</code> (<code>TextIOWrapper</code>)</li>
    <li><code>dump</code> (<code>function</code>) (from 'json')</li>
    <li><code>dumps</code> (<code>function</code>) (from 'json')</li>
    <li><code>load</code> (<code>function</code>) (from 'json')</li>
    <li><code>loads</code> (<code>function</code>) (from 'json')</li>
    <li><code>JSONDecoder</code> (<code>type</code>) (from 'json.decoder')</li>
    <li><code>JSONDecodeError</code> (<code>type</code>) (from 'json.decoder')</li>
    <li><code>JSONEncoder</code> (<code>type</code>) (from 'json.encoder')</li>
    <li><code>all</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>any</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>ascii</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>bin</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>breakpoint</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>callable</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>chr</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>delattr</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>dir</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>divmod</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>eval</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>exec</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>format</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>getattr</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>globals</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>hasattr</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>hash</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>hex</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>id</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>input</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>isinstance</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>issubclass</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>iter</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>aiter</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>len</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>locals</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>max</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>min</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>next</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>anext</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>oct</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>ord</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>print</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>repr</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>round</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>setattr</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>sorted</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>sum</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>vars</code> (<code>builtin_function_or_method</code>) (from 'builtins')</li>
    <li><code>None</code> (<code>NoneType</code>)</li>
    <li><code>Ellipsis</code> (<code>ellipsis</code>)</li>
    <li><code>NotImplemented</code> (<code>NotImplementedType</code>)</li>
    <li><code>False</code> (<code>bool</code>)</li>
    <li><code>True</code> (<code>bool</code>)</li>
    <li><code>bool</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>memoryview</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>bytearray</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>bytes</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>classmethod</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>complex</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>dict</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>enumerate</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>filter</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>float</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>frozenset</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>property</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>int</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>list</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>map</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>object</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>range</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>reversed</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>set</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>slice</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>staticmethod</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>str</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>super</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>tuple</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>type</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>zip</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>BaseException</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>BaseExceptionGroup</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>Exception</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>GeneratorExit</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>KeyboardInterrupt</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>SystemExit</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ArithmeticError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>AssertionError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>AttributeError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>BufferError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>EOFError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ImportError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>LookupError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>MemoryError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>NameError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>OSError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ReferenceError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>RuntimeError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>StopAsyncIteration</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>StopIteration</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>SyntaxError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>SystemError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>TypeError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ValueError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>Warning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>FloatingPointError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>OverflowError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ZeroDivisionError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>BytesWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>DecodecationWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>EncodingWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>FutureWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ImportWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>PendingDecodecationWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ResourceWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>RuntimeWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>SyntaxWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>UnicodeWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>UserWarning</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ChildProcessError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ConnectionError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>FileExistsError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>FileNotFoundError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>InterruptedError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>IsADirectoryError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>NotADirectoryError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>PermissionError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ProcessLookupError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>TimeoutError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>IndentationError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>IndexError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>KeyError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ModuleNotFoundError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>NotImplementedError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>RecursionError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>UnboundLocalError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>UnicodeError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>BrokenPipeError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ConnectionAbortedError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ConnectionRefusedError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ConnectionResetError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>TabError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>UnicodeDecodeError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>UnicodeEncodeError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>UnicodeTranslateError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>ExceptionGroup</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>EnvironmentError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>IOError</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>quit</code> (<code>Quitter</code>) (from '_sitebuiltins')</li>
    <li><code>credits</code> (<code>_Printer</code>) (from '_sitebuiltins')</li>
    <li><code>license</code> (<code>_Printer</code>) (from '_sitebuiltins')</li>
    <li><code>help</code> (<code>_Helper</code>) (from '_sitebuiltins')</li>
    <li><code>string</code> (<code>module</code>)</li>
    <li><code>re</code> (<code>module</code>)</li>
    <li><code>collections</code> (<code>module</code>)</li>
    <li><code>heapq</code> (<code>module</code>)</li>
    <li><code>math</code> (<code>module</code>)</li>
    <li><code>statistics</code> (<code>module</code>)</li>
    <li><code>itertools</code> (<code>module</code>)</li>
    <li><code>functools</code> (<code>module</code>)</li>
    <li><code>operator</code> (<code>module</code>)</li>
    <li><code>io</code> (<code>module</code>)</li>
    <li><code>sys</code> (<code>module</code>)</li>
    <li><code>json</code> (<code>module</code>)</li>
    <li><code>codecompiled</code> (<code>module</code>)</li>
    <li><code>DeserializeError</code> (<code>type</code>) (from 'codecompiled.__deserializer__')</li>
    <li><code>ListNode</code> (<code>type</code>) (from 'codecompiled.listnode')</li>
    <li><code>NestedInteger</code> (<code>type</code>) (from 'codecompiled.nestedinteger')</li>
    <li><code>TreeNode</code> (<code>type</code>) (from 'codecompiled.treenode')</li>
    <li><code>Annotated</code> (<code>type</code>) (from 'typing')</li>
    <li><code>Any</code> (<code>_AnyMeta</code>) (from 'typing')</li>
    <li><code>Callable</code> (<code>_CallableType</code>) (from 'typing')</li>
    <li><code>ClassVar</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>Concatenate</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>Final</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>ForwardRef</code> (<code>type</code>) (from 'typing')</li>
    <li><code>Generic</code> (<code>type</code>) (from 'typing')</li>
    <li><code>Literal</code> (<code>_LiteralSpecialForm</code>) (from 'typing')</li>
    <li><code>Optional</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>ParamSpec</code> (<code>type</code>) (from 'typing')</li>
    <li><code>Protocol</code> (<code>_ProtocolMeta</code>) (from 'typing')</li>
    <li><code>Tuple</code> (<code>_TupleType</code>) (from 'typing')</li>
    <li><code>Type</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>TypeVar</code> (<code>type</code>) (from 'typing')</li>
    <li><code>TypeVarTuple</code> (<code>type</code>) (from 'typing')</li>
    <li><code>Union</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>AbstractSet</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>ByteString</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Container</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>ContextManager</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Hashable</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>ItemsView</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Iterable</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Iterator</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>KeysView</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Mapping</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>MappingView</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>MutableMapping</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>MutableSequence</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>MutableSet</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Sequence</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Sized</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>ValuesView</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Awaitable</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>AsyncIterator</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>AsyncIterable</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Coroutine</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Collection</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>AsyncGenerator</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>AsyncContextManager</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Reversible</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>SupportsAbs</code> (<code>_ProtocolMeta</code>) (from 'typing')</li>
    <li><code>SupportsBytes</code> (<code>_ProtocolMeta</code>) (from 'typing')</li>
    <li><code>SupportsComplex</code> (<code>_ProtocolMeta</code>) (from 'typing')</li>
    <li><code>SupportsFloat</code> (<code>_ProtocolMeta</code>) (from 'typing')</li>
    <li><code>SupportsIndex</code> (<code>_ProtocolMeta</code>) (from 'typing')</li>
    <li><code>SupportsInt</code> (<code>_ProtocolMeta</code>) (from 'typing')</li>
    <li><code>SupportsRound</code> (<code>_ProtocolMeta</code>) (from 'typing')</li>
    <li><code>Deque</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Dict</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>DefaultDict</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>List</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>Set</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>FrozenSet</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>NamedTuple</code> (<code>function</code>) (from 'typing')</li>
    <li><code>TypedDict</code> (<code>function</code>) (from 'typing')</li>
    <li><code>Generator</code> (<code>_SpecialGenericAlias</code>) (from 'typing')</li>
    <li><code>BinaryIO</code> (<code>type</code>) (from 'typing')</li>
    <li><code>IO</code> (<code>type</code>) (from 'typing')</li>
    <li><code>TextIO</code> (<code>type</code>) (from 'typing')</li>
    <li><code>AnyStr</code> (<code>TypeVar</code>) (from 'typing')</li>
    <li><code>assert_type</code> (<code>function</code>) (from 'typing')</li>
    <li><code>assert_never</code> (<code>function</code>) (from 'typing')</li>
    <li><code>cast</code> (<code>function</code>) (from 'typing')</li>
    <li><code>clear_overloads</code> (<code>function</code>) (from 'typing')</li>
    <li><code>dataclass_transform</code> (<code>function</code>) (from 'typing')</li>
    <li><code>final</code> (<code>function</code>) (from 'typing')</li>
    <li><code>get_args</code> (<code>function</code>) (from 'typing')</li>
    <li><code>get_origin</code> (<code>function</code>) (from 'typing')</li>
    <li><code>get_overloads</code> (<code>function</code>) (from 'typing')</li>
    <li><code>get_type_hints</code> (<code>function</code>) (from 'typing')</li>
    <li><code>is_typeddict</code> (<code>function</code>) (from 'typing')</li>
    <li><code>LiteralString</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>Never</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>NewType</code> (<code>type</code>) (from 'typing')</li>
    <li><code>no_type_check</code> (<code>function</code>) (from 'typing')</li>
    <li><code>no_type_check_decorator</code> (<code>function</code>) (from 'typing')</li>
    <li><code>NoReturn</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>NotRequired</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>overload</code> (<code>function</code>) (from 'typing')</li>
    <li><code>ParamSpecArgs</code> (<code>type</code>) (from 'typing')</li>
    <li><code>ParamSpecKwargs</code> (<code>type</code>) (from 'typing')</li>
    <li><code>Required</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>reveal_type</code> (<code>function</code>) (from 'typing')</li>
    <li><code>runtime_checkable</code> (<code>function</code>) (from 'typing')</li>
    <li><code>Self</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>Text</code> (<code>type</code>) (from 'builtins')</li>
    <li><code>TYPE_CHECKING</code> (<code>bool</code>)</li>
    <li><code>TypeAlias</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>TypeGuard</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>Unpack</code> (<code>_SpecialForm</code>) (from 'typing')</li>
    <li><code>Solution</code> (<code>type</code>) (from '__main__')</li>
    <li><code>os</code> (<code>module</code>)</li>
    <li><code>_driver</code> (<code>function</code>) (from '__main__')</li>
</ul>
</details>

## Header

Here is the header included in the solution file:

```python
"""
This file is automatically generated and emulates the Python environment on the LeetCode platform.
"""

# Wildcard imports
from string import *
from re import *
from operator import *
from builtins import *
from typing import *
from datetime import *
from collections import *
from heapq import *
from bisect import *
from copy import *
from math import *
from random import *
from statistics import *
from itertools import *
from functools import *
from io import *
from sys import *
from json import *

# Module imports
import datetime
import bisect
import copy
import random
import string
import re
import collections
import heapq
import math
import statistics
import itertools
import functools
import operator
import io
import sys
import json
import os

# Disclaimer: The `_driver` is missing but this is an internal module which shouldn't be used in any problem.

# Defining the `precompiled` module, which contains some LeetCode custom datastructures.

class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

class NestedInteger:
    def __init__(self, value=None):
        self.value = value

    def isInteger(self) -> bool:
        return isinstance(self.value, int)

    def getInteger(self) -> int:
        return self.value

    def getList(self) -> List['NestedInteger']:
        return self.value

class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

# Creating a module to store the custom datastructures
import types
precompiled = types.ModuleType('precompiled', 'A module containing LeetCode custom datastructures.')
# Updating the module with the custom datastructures
precompiled.__dict__.update({
    'ListNode': ListNode,
    'NestedInteger': NestedInteger,
    'TreeNode': TreeNode,
})
# Adding it to the list of modules
sys.modules['precompiled'] = precompiled
# The `types` module is not in the default LeetCode Python environment.
del types

# Redefining the different datastructures for them to have the same behavior as module elements
ListNode = precompiled.ListNode
NestedInteger = precompiled.NestedInteger
TreeNode = precompiled.TreeNode
```

> [!NOTE]  
> This template takes no arguments.

## Footer

Here is the footer included in the solution file:

```python
# We could expect that the user decided to remove these imports for whatever reason
from typing import List, Tuple, Dict, Any
# Defining the tests
__LEETCODE_{run_id}_TESTS__: List[Tuple[Dict[str, Any], Any]] = {tests}
#                                       ^^^^^^^^^^^^^^  ^^^
#                                      Input arguments  Expected
#                                                       output
# Example: __LEETCODE_0_TESTS__ = [({{'val1': 1, 'val2': 2}}, 3)]

def __LEETCODE_{run_id}_RUN_TESTS__():
    """Runs the tests for the solution."""
    NUMBER_OF_TESTS = str(len(__LEETCODE_{run_id}_TESTS__))
    TEST_MESSAGE_TEMPLATE = f"Test {{{{index}}}}/{{NUMBER_OF_TESTS}}... "
    INDEX_JUSTIFICATION = len(NUMBER_OF_TESTS)
    MAX_MESSAGE_LENGTH = len(TEST_MESSAGE_TEMPLATE.format(index=NUMBER_OF_TESTS))
    for index, (test, expected) in enumerate(__LEETCODE_{run_id}_TESTS__, start=1):
        testing_message = TEST_MESSAGE_TEMPLATE.format(index=str(index).ljust(INDEX_JUSTIFICATION))
        print(testing_message, end="\r")
        sol = {function_name}(
            **test
        )
        if sol != expected:
            print(testing_message + "FAILED ⛑️".rjust(MAX_MESSAGE_LENGTH - len(testing_message)))
            print("Given input", test)
            print("Got", sol)
            print("Expected", expected)
            exit(1)
        else:
            print(testing_message + "PASSED ✅".rjust(MAX_MESSAGE_LENGTH - len(testing_message)))
    print("✨ All tests passed! 🙆")

if __name__ == "__main__":
    # Running the solution
    __LEETCODE_{run_id}_RUN_TESTS__()
```

> [!NOTE]  
> This template takes the following arguments:
>
> - `run_id`: The unique identifier of the run.
> - `tests`: The list of tests to run.
> - `function_name`: The name of the function to test.
